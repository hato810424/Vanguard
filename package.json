{
  "name": "Vanguard",
  "scripts": {
    "db:push": "pnpm --filter=auth db:push",
    "build": "turbo build",
    "dev": "turbo dev",
    "start": "pnpm --filter=auth start"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "pnpm": ">=10.30.0"
  },
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}
